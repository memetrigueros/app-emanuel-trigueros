export interface Product {
    id: string;
    name: string;
    description: string;
    price: number;
    imageUrl: string;
    stockQuantity: number;
    quantity: number;
    createdAt: string,
    updatedAt: string
}