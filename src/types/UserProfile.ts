export interface UserProfile {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    address: string;
    birthDate: string,
    createdAt: string,
    updatedAt: string
}